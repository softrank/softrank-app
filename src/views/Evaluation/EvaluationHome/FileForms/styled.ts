import styled from 'styled-components';

export const StepDetails = styled.span`
  color: var(--gray-700);
  font-size: 1.2rem;
`;

export const ImageContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
`;
