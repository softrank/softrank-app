import styled from 'styled-components';

export default styled.thead`
  text-align: left;
  font-size: 1.3rem;
  font-weight: 500;
  padding: 1em;
  border-bottom: 2px solid red;
  color: var(--accent);
  border-radius: 4px;
`;
