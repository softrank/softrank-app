import styled from 'styled-components';

export default styled.thead`
  text-align: left;
  font-size: 1.3rem;
  font-weight: 500;

  color: var(--dark-purple);

  > tr > td {
    padding: 0.6em;
  }
`;
