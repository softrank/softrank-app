import styled from 'styled-components';
import Button from './Button';

export const ButtonSeconday = styled(Button)`
  background: var(--light-purple);
  color: var(--dark-purple);
  border: 2px solid var(--light-purple);
`;
